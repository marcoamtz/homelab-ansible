# Real config files (contain secrets) — only .example versions are committed
group_vars/*.yml
!group_vars/*.yml.example
inventory.ini

# Ansible
*.retry
*.vault.yml

# Secrets & keys
.env
.env.*
*.pem
*.key
*.crt
vault.yml
secrets/

# OS
.DS_Store
