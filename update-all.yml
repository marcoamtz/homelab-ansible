- name: Update all LXC containers
  hosts: dns_servers,tailscale_nodes,docker_hosts
  become: yes

  tasks:
    - name: Update and upgrade packages
      apt:
        update_cache: yes
        upgrade: dist
