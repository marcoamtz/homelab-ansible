---
- name: Deploy Tailscale Bridge (LXC Optimized)
  hosts: tailscale_nodes
  become: true

  roles:
    - role: common
      vars:
        common_extra_packages: [locales, ca-certificates]
    - tailscale_node
