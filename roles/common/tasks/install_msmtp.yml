---
- name: Install msmtp
  ansible.builtin.apt:
    name: msmtp
    state: present
  tags: [install]

- name: Deploy msmtp configuration
  ansible.builtin.template:
    src: msmtprc.j2
    dest: /etc/msmtprc
    owner: root
    group: root
    mode: "0600"
  tags: [config]
